# Codex estate upgrade payload — 2026-09-20

Fail-closed handoff. Production stays **HOLD**. This organ is `szl-holdings/szl-frontier`, not a second flagship.

## Honest census (membership only)

Live public probes on 2026-09-20:

| Family | Status | Count | Note |
| --- | --- | --- | --- |
| GitHub public org repos | MEASURED | 117 | Authenticated search earlier saw 124; remainder unpublished. Do not print UNKNOWN as 0. |
| HF models | MEASURED | 47 | Membership ≠ qualification. |
| HF datasets | MEASURED | 35 | Membership ≠ training admission. |
| HF spaces | MEASURED | 21 | RUNNING ≠ runtime-verified. |
| HF kernels | MEASURED | 14 | Separate family. IDs may overlap models. |

`source_content_files_read=0`. `file_audit_complete=false`. Semantic review was **not** performed. Tree hashing, LFS, and private assets were not inspected. A repo-by-repo file audit of 117 repositories was not executed and must not be claimed.

UNKNOWN ≠ 0. RUNNING ≠ runtime-verified. health ≠ ready ≠ source. kernels ≠ models. Λ = Conjecture 1. Training off. $0.

## What this organ executes

THIS_ORGAN: FE-03, FE-06 (observe only), DR-01, DR-03 (contract + display), DR-05, DR-06, DR-07, DR-08, DR-10.

HANDOFF to existing owners: a11oy, a11oy-net, szl-brand, szl-serve, szl-router, szl-forge, substrate, holograms, verticals. Do not edit those mains from this payload. Do not resurrect archived holograms. Do not unify frontends.

## Shipped in this worktree

- `/api/health` `/api/ready` `/api/source` `/api/triad` — distinct schemas, productionReady always false
- `/api/estate` — live membership including kernels family + model-id overlap
- `/api/pin` — MEMBERSHIP_IDENTITY_PIN only; HEAD_SHA_PIN refused without revisions
- `/api/codex` — 26-lane program, THIS_ORGAN vs HANDOFF
- Frontier tabs: estate, triad, pin, codex, lab
- Python second-reader: `python/payload/szl_codex_upgrade.py`

## Codex rules

Isolated worktrees. Draft PRs only. Never merge protected main from this payload. Never recreate a11oy. Never reset owner dirs. Promotion cannot lift HOLD.

## Frontier that is still legal under HOLD

Receipt chrome on every operator surface. Space triad contract without mutating 21 Space repos. Membership pin heartbeat that refuses to become a fake file audit. Kernel family kept distinct even when Hub IDs collide with models. Λ stays Conjecture 1.
